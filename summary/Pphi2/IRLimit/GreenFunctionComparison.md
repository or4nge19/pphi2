# `GreenFunctionComparison.lean` — Informal Summary

> **Source**: [`Pphi2/IRLimit/GreenFunctionComparison.lean`](../../../Pphi2/IRLimit/GreenFunctionComparison.lean)
> **Generated**: 2026-03-20
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

Provides the uniform second moment bound for the cylinder pullback measures using the method of images from the gaussian-field library. The proof chain is: (1) pullback identity $\mathbb{E}_{\nu_{L_t}}[(\omega f)^2] = \mathbb{E}_{\mu_{L_t}}[(\omega(\mathrm{embed}\, f))^2]$; (2) density transfer from the OS1 regularity of the torus measure; (3) method-of-images bound $G_{L_t,L_s}(\mathrm{embed}\, f, \mathrm{embed}\, f) \le C'\, q(f)^2$ uniform in $L_t$. This is the core tightness input for the IR limit.

## Status
**Main result**: 1 axiom (`cylinderIR_uniform_second_moment`), 0 sorry(s)
**Length**: 65 lines, 0 definitions + 0 theorems + 1 axiom

---

### `cylinderIR_uniform_second_moment` (axiom)

```lean
axiom cylinderIR_uniform_second_moment
    (P : InteractionPolynomial) (mass : ℝ) (hmass : 0 < mass) :
    ∃ (C : ℝ) (q : Seminorm ℝ (CylinderTestFunction Ls)),
    0 < C ∧ Continuous q ∧
    ∀ (Lt : ℝ) [Fact (0 < Lt)] (_ : 1 ≤ Lt)
      (μ : Measure (Configuration (AsymTorusTestFunction Lt Ls)))
      [hμ : IsProbabilityMeasure μ]
      (_ : @AsymSatisfiesTorusOS Lt Ls _ _ μ hμ)
      (f : CylinderTestFunction Ls),
    ∫ ω : Configuration (CylinderTestFunction Ls),
      (ω f) ^ 2 ∂(cylinderPullbackMeasure Lt Ls μ) ≤
    C * q f ^ 2
```

**Informal statement**: There exist constants $C > 0$ and a continuous seminorm $q$ on the cylinder test function space such that for all $L_t \ge 1$ and all interacting torus measures $\mu$ satisfying OS0--OS2,
$$\int (\omega(f))^2\, d\nu_{L_t} \le C\, q(f)^2$$
uniformly in $L_t$.

**Proof route**: Combine the pullback identity, torus regularity (OS1), and the method-of-images bound `torusGreen_uniform_bound` from the gaussian-field library (Simon, Ch. VIII).

---
*This file has **0** definitions and **0** theorems/lemmas + **1** axiom (0 with sorry).*
