# `AsymTorusOS.lean` — Informal Summary

> **Source**: [`Pphi2/AsymTorus/AsymTorusOS.lean`](../../../Pphi2/AsymTorus/AsymTorusOS.lean)
>
> **Generated**: 2026-03-20
>
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

States and proves OS0--OS2 for the asymmetric torus interacting continuum limit, following the same structure as `TorusInteractingOS.lean`. All proofs follow identical patterns to the symmetric torus case, with `asymGeomSpacing Lt Ls N` replacing `circleSpacing L N`. OS0 (analyticity) is proved via `analyticOnNhd_integral` with exponential moment domination. OS1 (regularity) follows from the exponential moment bound. OS2 (translation and time reflection invariance) is proved by transferring lattice symmetries through the weak limit via lattice approximation and unique limits.

## Status
**Main result**: Fully proven, 0 axiom(s), 0 sorry(s)
**Length**: 2519 lines, 5 definitions + 27 theorems/lemmas (many private) + 1 structure

---

### `asymTorusGeneratingFunctional`

```lean
def asymTorusGeneratingFunctional
    (μ : Measure (Configuration (AsymTorusTestFunction Lt Ls)))
    [IsProbabilityMeasure μ] (f : AsymTorusTestFunction Lt Ls) : ℂ
```

The generating (characteristic) functional on the asymmetric torus: $Z_\mu(f) = \int \exp(i\, \omega(f))\, d\mu$.

---

### `AsymTorusOS0_Analyticity`

```lean
def AsymTorusOS0_Analyticity
    (μ : Measure (Configuration (AsymTorusTestFunction Lt Ls)))
    [IsProbabilityMeasure μ] : Prop
```

OS0: The multi-variable generating functional $(z_1, \ldots, z_n) \mapsto \int \exp(i\, \omega(\sum_i (\Re z_i) J_i + i\, (\Im z_i) J_i))\, d\mu$ is analytic on $\mathbb{C}^n$.

---

### `AsymTorusOS1_Regularity`

```lean
def AsymTorusOS1_Regularity
    (μ : Measure (Configuration (AsymTorusTestFunction Lt Ls)))
    [IsProbabilityMeasure μ] : Prop
```

OS1: The generating functional is bounded by $\|Z_\mu(f_{\mathrm{re}}, f_{\mathrm{im}})\| \le \exp(c\, (q(f_{\mathrm{re}}) + q(f_{\mathrm{im}})))$ for a continuous function $q$ and constant $c > 0$.

---

### `AsymTorusOS2_TranslationInvariance`

```lean
def AsymTorusOS2_TranslationInvariance
    (μ : Measure (Configuration (AsymTorusTestFunction Lt Ls)))
    [IsProbabilityMeasure μ] : Prop
```

OS2 translation invariance: $Z_\mu(f) = Z_\mu(T_v f)$ for all $v \in \mathbb{R}^2$.

---

### `AsymTorusOS2_TimeReflectionInvariance`

```lean
def AsymTorusOS2_TimeReflectionInvariance
    (μ : Measure (Configuration (AsymTorusTestFunction Lt Ls)))
    [IsProbabilityMeasure μ] : Prop
```

OS2 time reflection: $Z_\mu(f) = Z_\mu(\Theta f)$ where $\Theta: (t,x) \mapsto (-t,x)$.

---

### `AsymSatisfiesTorusOS` (structure)

```lean
structure AsymSatisfiesTorusOS
    (μ : Measure (Configuration (AsymTorusTestFunction Lt Ls)))
    [IsProbabilityMeasure μ] where
  os0 : AsymTorusOS0_Analyticity Lt Ls μ
  os1 : AsymTorusOS1_Regularity Lt Ls μ
  os2_translation : AsymTorusOS2_TranslationInvariance Lt Ls μ
  os2_timeReflection : AsymTorusOS2_TimeReflectionInvariance Lt Ls μ
```

Bundled OS axioms for the asymmetric torus.

---

### `asymInteractingLatticeMeasure_symmetry_invariant` (private)

**Informal statement**: For any bijection $\sigma$ on lattice sites that preserves the Gaussian density, $\int F(\omega \circ \sigma)\, d\mu_{\mathrm{int}} = \int F(\omega)\, d\mu_{\mathrm{int}}$.

**Proof**: Fully proved by unfolding the interacting measure as $Z^{-1} \cdot \mu_{\mathrm{GFF}} \cdot e^{-V}$, using that both the Gaussian density and the interaction are invariant under $\sigma$.

---

### `asymFiniteLaplacian_timeReflect_commute` (private)

**Informal statement**: The finite Laplacian commutes with time reflection: $\Delta(\varphi \circ \Theta) = (\Delta \varphi) \circ \Theta$.

**Proof**: Fully proved by extensionality and `fin_cases` on the direction index.

---

### `asymMassOperator_timeReflect_commute` (private)

**Informal statement**: The mass operator $(-\Delta + m^2)$ commutes with time reflection.

**Proof**: Fully proved from `asymFiniteLaplacian_timeReflect_commute`.

---

### `asymInteractingLatticeMeasure_timeReflection_invariant` (private)

**Informal statement**: The interacting lattice measure with any spacing $a$ is time-reflection invariant.

**Proof**: Fully proved by combining `asymInteractingLatticeMeasure_symmetry_invariant` with the time-reflection density preservation argument.

---

### `evalAsymAtFinSite_timeReflection` (private)

**Informal statement**: $\mathrm{eval}_x(\Theta f) = \mathrm{eval}_{\Theta x}(f)$ -- evaluation is equivariant under time reflection.

**Proof**: Fully proved using `evalCLM_comp_mapCLM` and the key identity that circle restriction composed with circle reflection gives evaluation at the reflected point.

---

### `asymTorusInteractingMeasure_gf_timeReflection_invariant` (private)

**Informal statement**: The cutoff generating functional is time-reflection invariant: $Z_N(f) = Z_N(\Theta f)$.

**Proof**: Fully proved from `evalAsymAtFinSite_timeReflection` and `asymInteractingLatticeMeasure_timeReflection_invariant`.

---

### `asymGaussian_exp_abs_moment` (private)

**Informal statement**: $\int \exp(t|\omega(g)|)\, d\mu_{\mathrm{GFF}} \le 2 \exp\bigl(\tfrac{t^2}{2} \int (\omega g)^2\, d\mu_{\mathrm{GFF}}\bigr)$.

**Proof**: Fully proved via the Gaussian pushforward `pairing_is_gaussian`, splitting $\exp(t|x|) \le \exp(tx) + \exp(-tx)$, and using Gaussian moment generating function bounds.

---

### `asymTorusInteractingMeasure_exponentialMomentBound_cutoff` (private)

**Informal statement**: Cutoff-level exponential moment bound: $\int \exp(|\omega(f)|)\, d\mu_{P,N} \le \sqrt{2K} \cdot \exp(\sigma^2_{\mathrm{GFF},N}(f))$ uniformly in $N$, where $K$ is from Nelson's estimate.

**Proof**: Fully proved using `asymNelson_exponential_estimate`, `asymGaussian_exp_abs_moment`, `density_transfer_bound`, and Cauchy--Schwarz.

---

### `asymTorusInteracting_exponentialMomentBound` (private)

**Informal statement**: The continuum limit measure has an exponential moment bound $\int \exp(|\omega(f)|)\, d\mu \le \exp(q(f))$ for a continuous function $q$.

**Proof**: Fully proved by constructing $q$ from the uniform Gaussian second moment bound and transferring cutoff bounds through the weak limit.

---

### `asymTorusInteracting_os0` (private)

**Informal statement**: OS0 analyticity for the limit measure.

**Proof**: Fully proved via `analyticOnNhd_integral` with pointwise analyticity of $\exp$ and domination from `asymTorusInteracting_exponentialMomentBound`. Uses `asymCompact_im_bound` and `asymExp_mul_sum_le` as helpers.

---

### `asymTorusInteracting_os1` (private)

**Informal statement**: OS1 regularity: $\|Z_\mu(f_{\mathrm{re}}, f_{\mathrm{im}})\| \le \exp(q(f_{\mathrm{re}}) + q(f_{\mathrm{im}}))$.

**Proof**: Fully proved from `asymTorusInteracting_exponentialMomentBound` via the triangle inequality, $\|\exp(i(x + iy))\| = e^{-y}$, and monotonicity of $\exp$.

---

### `evalAsymAtFinSite_latticeTranslation` (private)

**Informal statement**: $\mathrm{eval}_x(T_{(j_1 a_t, j_2 a_s)} f) = \mathrm{eval}_{x - (j_1, j_2)}(f)$ -- evaluation is equivariant under lattice translations.

**Proof**: Fully proved using `evalCLM_comp_mapCLM` and `circleRestriction_comp_circleTranslation`.

---

### `asymInteractingLatticeMeasure_translation_invariant` (private)

**Informal statement**: The interacting lattice measure is translation-invariant under lattice shifts.

**Proof**: Fully proved via `asymInteractingLatticeMeasure_symmetry_invariant` with translation bijectivity and `gaussianDensity_translation_invariant`.

---

### `asymTorusInteractingMeasure_gf_latticeTranslation_invariant`

```lean
theorem asymTorusInteractingMeasure_gf_latticeTranslation_invariant
    (N : ℕ) [NeZero N] (P : InteractionPolynomial) (mass : ℝ) (hmass : 0 < mass)
    (j₁ j₂ : ℤ) (f : AsymTorusTestFunction Lt Ls) :
    asymTorusGeneratingFunctional Lt Ls (asymTorusInteractingMeasure Lt Ls N P mass hmass) f =
    asymTorusGeneratingFunctional Lt Ls (asymTorusInteractingMeasure Lt Ls N P mass hmass)
      (asymTorusTranslation Lt Ls (circleSpacing Lt N * j₁, circleSpacing Ls N * j₂) f)
```

**Informal statement**: The cutoff generating functional is invariant under lattice translations: $Z_N(f) = Z_N(T_{(j_1 a_t, j_2 a_s)} f)$.

**Proof**: Fully proved from `evalAsymAtFinSite_latticeTranslation` and `asymInteractingLatticeMeasure_translation_invariant`.

---

### `asymGf_sub_norm_le_seminorm`

```lean
theorem asymGf_sub_norm_le_seminorm
    (P : InteractionPolynomial) (mass : ℝ) (hmass : 0 < mass) :
    ∃ (B : ℝ) (p : AsymTorusTestFunction Lt Ls → ℝ),
    Continuous p ∧ p 0 = 0 ∧
    ∀ (N : ℕ) [NeZero N] (g h : AsymTorusTestFunction Lt Ls),
    ‖Z_N(g) - Z_N(h)‖ ≤ B * p (g - h)
```

**Informal statement**: Uniform Lipschitz bound on the cutoff generating functional: $\|Z_N(g) - Z_N(h)\| \le B \cdot p(g - h)$ with $p$ continuous and $p(0) = 0$.

**Proof**: Fully proved via Cauchy--Schwarz, mean value theorem for $\sin/\cos$, density transfer, hypercontractivity, and the tight Riemann sum bound `asymLatticeTestFn_norm_sq_tight`.

**Dependencies**: `asymLatticeTestFn_norm_sq_tight`, `density_transfer_bound`, `gaussian_hypercontractive`, `asymNelson_exponential_estimate`.

---

### `circleTranslation_continuous_in_s` (private)

**Informal statement**: The map $s \mapsto T_s g$ is continuous for each smooth circle function $g$, in the Sobolev seminorm topology.

**Proof**: Fully proved via uniform continuity of iterated derivatives of periodic functions on compact domains.

---

### `asymTorusTranslation_continuous_in_v`

```lean
theorem asymTorusTranslation_continuous_in_v
    (f : AsymTorusTestFunction Lt Ls) :
    Continuous (fun v : ℝ × ℝ => asymTorusTranslation Lt Ls v f)
```

**Informal statement**: The map $v \mapsto T_v f$ is continuous in the NTP topology.

**Proof**: Fully proved by reducing to `WithSeminorms` characterization, bounding NTP seminorms of $T_v f - T_{v_0} f$ via pure tensor bounds, and using `circleTranslation_continuous_in_s` for each factor.

**Dependencies**: `circleTranslation_continuous_in_s`, `NuclearTensorProduct.pure_seminorm_bound`, `RapidDecaySeq.rapidDecay_withSeminorms`.

---

### `asymTorusGF_latticeApproximation_error_vanishes`

```lean
theorem asymTorusGF_latticeApproximation_error_vanishes
    (P : InteractionPolynomial) (mass : ℝ) (hmass : 0 < mass)
    (φ : ℕ → ℕ) (hφ : StrictMono φ)
    (v : ℝ × ℝ) (f : AsymTorusTestFunction Lt Ls) :
    Tendsto (fun n => Z_{φ(n)+1}(T_v f) - Z_{φ(n)+1}(f)) atTop (nhds 0)
```

**Informal statement**: The difference $Z_N(T_v f) - Z_N(f) \to 0$ as $N \to \infty$, via lattice rounding and generating functional continuity.

**Proof**: Fully proved. Decomposes $Z_N(T_v f) - Z_N(f) = (Z_N(T_v f) - Z_N(T_{w_N} f)) + 0$ where $w_N$ is the nearest lattice point to $v$, uses `asymTorusInteractingMeasure_gf_latticeTranslation_invariant` for the second term and `asymGf_sub_norm_le_seminorm` with `asymTorusTranslation_continuous_in_v` for the first.

**Dependencies**: `asymGf_sub_norm_le_seminorm`, `asymTorusInteractingMeasure_gf_latticeTranslation_invariant`, `asymTorusTranslation_continuous_in_v`.

---

### `asymTorusInteracting_os2_translation` (private)

**Informal statement**: OS2 translation invariance for the continuum limit: $Z(f) = Z(T_v f)$ for all $v \in \mathbb{R}^2$.

**Proof**: Fully proved. Shows $Z_N(T_v f) \to Z(T_v f)$ and $Z_N(T_v f) - Z_N(f) \to 0$ (via `asymTorusGF_latticeApproximation_error_vanishes`), and deduces $Z(T_v f) = Z(f)$ by uniqueness of limits.

**Dependencies**: `asymTorusGF_latticeApproximation_error_vanishes`, `asymGf_re_eq_cos_integral`, `asymGf_im_eq_sin_integral`.

---

### `asymTorusInteracting_os2_timeReflection` (private)

**Informal statement**: OS2 time reflection invariance for the continuum limit: $Z(f) = Z(\Theta f)$.

**Proof**: Fully proved by transferring cutoff-level invariance (`asymTorusInteractingMeasure_gf_timeReflection_invariant`) through the weak limit via cos/sin decomposition and `tendsto_nhds_unique`.

**Dependencies**: `asymTorusInteractingMeasure_gf_timeReflection_invariant`, `asymGf_re_eq_cos_integral`, `asymGf_im_eq_sin_integral`.

---

### `asymTorusInteracting_satisfies_OS`

```lean
theorem asymTorusInteracting_satisfies_OS
    (P : InteractionPolynomial) (mass : ℝ) (hmass : 0 < mass)
    (μ : Measure (Configuration (AsymTorusTestFunction Lt Ls)))
    [IsProbabilityMeasure μ]
    (φ : ℕ → ℕ) (hφ : StrictMono φ)
    (hconv : ...) :
    AsymSatisfiesTorusOS Lt Ls μ
```

**Informal statement**: The asymmetric torus $P(\varphi)_2$ interacting continuum limit satisfies OS0--OS2 (analyticity, regularity, translation invariance, time reflection invariance).

**Proof**: Fully proved by assembling `asymTorusInteracting_os0`, `asymTorusInteracting_os1`, `asymTorusInteracting_os2_translation`, `asymTorusInteracting_os2_timeReflection`.

---
*This file has **5** definitions and **27** theorems/lemmas + **1** structure (0 with sorry).*
